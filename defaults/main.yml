# SPDX-License-Identifier: MIT
---
# defaults file for configure_networking

hostname_string: "devops"  # Set the hostname
network_renderer: "networkd"  # Use 'networkd' for servers, and 'NetworkManager' for desktops
